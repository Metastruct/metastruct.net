# pnpm does not run dependency build scripts unless they are allowed here.
# esbuild and unrs-resolver link their platform binaries this way, and vite
# needs both to build.
allowBuilds:
  "@parcel/watcher": true
  esbuild: true
  unrs-resolver: true
